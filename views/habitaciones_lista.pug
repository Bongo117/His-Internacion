extends layout

block content
  .contenido-box
    div.d-flex.justify-content-between.align-items-center.mb-4
        h2 🏥 Mapa de Habitaciones
        a.btn.btn-success(href="/habitaciones/nueva") + Nueva Habitación

    if habitaciones.length == 0
      .alert.alert-warning No hay habitaciones registradas.
    else
      .row
        each h in habitaciones
          .col-md-4.mb-4
            .card.h-100.shadow-sm
              .card-header.d-flex.justify-content-between.align-items-center.bg-light
                strong= h.ala
                span.badge.bg-dark Hab. #{h.numero}
              
              .card-body
                h6.card-subtitle.mb-3.text-muted Capacidad: #{h.capacidad} camas
                
                // --- LISTA DE CAMAS INTERACTIVAS ---
                if h.camas.length === 0
                    p.text-muted.small 🚫 Sin camas instaladas
                else
                    ul.list-group.list-group-flush
                        each cama in h.camas
                            li.list-group-item.d-flex.justify-content-between.align-items-center.px-0
                                // Información de la cama
                                div
                                    strong Cama #{cama.numero}: 
                                    
                                    if cama.estado === 'libre'
                                        span.badge.bg-success Libre
                                    else if cama.estado === 'ocupada'
                                        span.badge.bg-danger Ocupada
                                        div.small.text-primary.mt-1 👤 #{cama.apellido}
                                    else if cama.estado === 'higienizando'
                                        span.badge.bg-warning.text-dark Limpieza
                                
                                // Acciones para la cama
                                div
                                    if cama.estado === 'higienizando'
                                        form.d-inline(method="POST" action=`/habitaciones/cama/${cama.id_cama}/limpiar`)
                                            button.btn.btn-sm.btn-outline-warning(type="submit" title="Marcar como limpia") 🧹 Limpiar
                                    
                                    if cama.estado === 'libre'
                                        //- Botón para eliminar la cama (solo si está libre)
                                        form.d-inline.ms-1(method="POST" action=`/habitaciones/cama/${cama.id_cama}/eliminar`)
                                            button.btn.btn-sm.btn-outline-danger(type="submit" title="Eliminar cama") 🗑️

              .card-footer.bg-transparent.text-center
                if h.camas.length < h.capacidad
                    form(method="POST" action=`/habitaciones/${h.id_habitacion}/agregar_cama`)
                        button.btn.btn-sm.btn-outline-primary(type="submit") ➕ Instalar Cama
                else
                    small.text-muted Cupo máximo de camas alcanzado