extends layout

block content
  .contenido-box
    h2 ✏️ Editar Cama
    
    form(method="POST" action=`/camas/editar/${cama.id_cama}`)
      .mb-3
        label.form-label Número de Cama
        input.form-control(type="number" name="numero" value=cama.numero required)

      .mb-3
        label.form-label Estado Actual
        select.form-select(name="estado")
            option(value="libre" selected=cama.estado==='libre') 🟢 Libre
            option(value="ocupada" selected=cama.estado==='ocupada') 🔴 Ocupada
            option(value="higienizando" selected=cama.estado==='higienizando') 🧹 Higienizando
            option(value="reparacion" selected=cama.estado==='reparacion') 🔧 En Reparación

      button.btn.btn-primary(type="submit") Guardar Cambios
      a.btn.btn-link(href="/habitaciones") Cancelar
